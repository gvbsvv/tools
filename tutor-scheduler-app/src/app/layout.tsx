import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Tutor Scheduler - Manage Tutors, Students & Classes',
  description: 'Comprehensive tutor scheduling application for managing tutors, students, classes, and payments with notifications and reminders.',
  keywords: ['tutor', 'scheduling', 'education', 'classes', 'students', 'teachers', 'appointments'],
  authors: [{ name: 'Tutor Scheduler App' }],
  viewport: 'width=device-width, initial-scale=1',
  robots: 'index, follow',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="theme-color" content="#3b82f6" />
      </head>
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  )
}
